<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter injects INTERNET into the debug/profile manifests only, so a
         release build without this line has no network at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- "Use my location" on the add-address form (geolocator). Without these
         declared, requestPermission() returns denied immediately and the button
         can never work, no matter what the user taps. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Android 13+ requires this before any FCM notification can be shown. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="barakath"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- REQUIRED by image_cropper: the plugin launches uCrop's activity with
             an EXPLICIT intent, so it must be declared here or startCrop throws
             ActivityNotFoundException.

             That throw is not merely a failed crop — it KILLS THE APP. The
             plugin has already called startActivityForResult, so Android still
             delivers a RESULT_CANCELED back to onActivityResult, which replies
             on a MethodChannel result the catch block has already completed:
             "IllegalStateException: Reply already submitted", fatal, on the
             platform thread. Observed on a real device: profile photo, review
             photos and replacement photos all take the app down. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
