# Icons map

> EMPTY TEMPLATE — populate this by running `scripts/dump-components.js` against
> YOUR published ICON library file.
>
> LARGE FILE ONCE POPULATED — NEVER load fully into context. Always `grep` for
> the icon name and read only the matching lines. Regenerate whenever the icon
> library is republished.

| Icon name | Key | Variant properties | Notes |
|-----------|-----|--------------------|-------|
| _(run the dump to populate)_ | | | |
